Basics

1. اینپوت ای که فقط عدد بپذیرد :
   اگر اینپوت با type = number استفاده بشه همچنان کاربر میتونه (.) به عنوان عدد اعشار و حرف (e) بعنوان عدد نپر رو وارد کنه پس اگر بخواهیم فقط عدد مجاز باشه یک اینپوت با type = text می سازیم و به کمک JS ورودی رو محدود به عدد میکنیم. ( البته توجه کنین که محدود کردن ورودی با validation متفاوت هست).

البته می تونیم attribute ای به نام pattern اضافه کنیم مثل کد زیر و یک regex برای اینپوت فقط عدد براش تعیین کنیم اما این پترن از ورود حروف جلوگیری نمیکنه بلکه فقط موقع زدن دکمه Submit از حروف ایراد میگیره و میگه فقط عدد وارد کن :

<form>
    <input
      type="text"
      name="idCode"
      placeholder="national code"
      maxlength="10"
      pattern="^[0-9]*$" />
    <button type="submit">submit</button>
  </form>
2. برای تلفن از تایپ tel استفاده کنیم یا text ؟ فرقشون چیه؟ و inputMode چیه؟
در واقع inputmode نوع کیبورد مناسب رو در موبایل و تبلت نشون میده و وقتی از type = tel (برای بحث validation هر چند محدود) و inputmode = tel استفاده می کنیم داخل موبایل کاربر Numpad کیبورد باز میشه و UX بهتری داره اما در type = text کیبورد اصلی باز میشه.

3. اینپوت با type = hidden چه کاربردی داره؟
کاربردهای زیادی داره مثلا در دیجی کالا :
<form action="/checkout" method="POST">
<!-- شناسه محصول در سبد خرید -->
<input type="hidden" name="cart_id" value="cart_67890">

<!-- کد تخفیف کاربر -->
<input type="hidden" name="discount_code" value="DIGI1403">

<!-- روش پرداخت انتخاب‌شده -->
<input type="hidden" name="payment_method" value="online">

<!-- آدرس تحویل (از پروفایل کاربر) -->
<input type="hidden" name="delivery_address_id" value="addr_123">

<!-- کاربر فقط این را می‌بیند -->

<button type="submit">پرداخت نهایی</button>

</form>
یکی از ویژگی هاش برای جلوگیری از فعالیت ربات ها است ، مثلا سایتی داریم در ورودی سه تا اینپوت ایمیل و پسورد و یکی هم hidden داریم که کاربر hidden رو نمی بینه ، حالا یه ربات وارد میشه و هر سه تا اینپوت رو میبینه و پر میکنه و اینطوری مشخص میشه ربات وارد شده که سریعا تشخیص داده میشه و بلاکش می کنیم.
4. کاربرد autofocus چیه؟
برای ایجاد UX (تجربه کاربری) بهتر برای یک input موردنظر میتونیم از autofocus استفاده کنیم اینطوری همین که صفحه لود میشه کرسر داخل اینپوت موردنظر آماده تایپ کردن هست یا مثلا بعد از لود صفحه در موبایل فورا کیبورد باز میشه و اینطوری حس بهتری برای کاربر ایجاد میشه.

5. در بحث فونت swap و fallback چیه؟
   در واقع SWAP در CSS fonts یک ارزش برای ویژگی font-display است که روی نحوه لود فونت‌های سفارشی (مثلاً از طریق @font-face) تاثیر میذاره. وقتی از فونت سفارشی استفاده می‌کنید، swap به مرورگر میگه:

"اول متن رو با فونت fallback نشون بده، بعد که فونت اصلی لود شد، عوضش کن"

@font-face {
font-family: 'MyFont';
src: url('myfont.woff2');
font-display: swap; /_ اینجا _/
}

body {
font-family: 'MyFont', Arial, sans-serif;
}
زمان با swap بدون swap
صفحه لود میشه فونت fallback (مثلاً Arial) نمایش داده میشه ممکنه متن نامرئی باشه (FOIT)
فونت اصلی لود شد بلافاصله به فونت اصلی عوض میشه به فونت اصلی عوض میشه
کاربر می‌بینه همیشه متن رو سریع می‌بینه ممکنه تأخیر در دیدن متن داشته باشه
فرض کن رستورانی که:

بدون swap: در رو بسته نگه می‌داره تا غذای اصلی آماده بشه (مشتری منتظر می‌مونه)
با swap: اول سالاد (fallback) می‌ده، بعد غذای اصلی (فونت سفارشی) رو میاره
در یک کلام: swap = "اول نشون بده، بعد عوض کن" - برای UX بهتر

Advanced

1. نصب node.js :
   برای نصب nodejs اولین راه دانلود نسخه Stable از سایت خود nodejs هست به نسخه های stable اصطلاحا LTS = Long Term Support هم میگن. nodejs در واقع یک محیط برای اجرای کدهای جاوااسکریپت روی سیستم است خب حالا مثلا ما یه نسخه از node رو نصب کردیم اما یک پروژه هست که نسخه 23 رو لازم داره یا پروژه ای نسخه 17 رو میخواد در این صورت باید بریم نود نصب شده رو پاک کنیم و نسخه موردنیاز رو از سایت نصب کنیم اما این کار درستی نیست و نمیشه هربار این کار رو تکرار کرد! برای رفع این مساله از NVM = Node Version Manager استفاده می کنیم. با NVM می تونیم همزمان چند نسخه از node رو روی سیستم داشته باشیم و بسته به نیازمان یکی را فعال میکنیم.

2. نصب NVM
   برای نصب nvm در گوگل nvm windows رو سرچ می کنیم و به قسمت release گیت هاب مربوطه و بخش latest میریم و از اونجا فایل exe رو دانلود و نصب می کنیم. بعد در powershell وقتی می زنیم nvm$ می بینیم که نسخه مورد نظر نصب شده. بعد با زدن nvm current$ نسخه node ای که موجود و نصب شده هست رو نشون میده. برای نصب نسخه استیبل یا همون lts Node.js دستور nvm install lts$ رو میزنیم. وقتی نصب تموم میشه حتما دستور nvm use 24.12.0 مرحله نصب و استفاده از نسخه Node.js lts رو کامل می کنیم. حالا وقتی دستور node رو وارد کنیم می بینیم که نود ورژن v24.12.0 در حال استفاده است.

فرض کنیم برای یک برنامه به نسخه دیگری از node مثلا 22.21.1 احتیاج داریم در این صورت با دستور nvm install 22.21.1 نسخه موردنظر رو نصب می کنیم. بعد هم با دستور nvm use 22.21.1 نصب و استفاده رو کامل می کنیم.

اگر بخواهیم لیست nvm های موجود رو ببینیم با دستور nvm list یا nvm ls این کار انجام میشه. آپ گرید و نصب مجدد و حذف نسخه های node هم از طریق راهنمای nvm قابل انجام هست.

3. درباره package manager ها (npm - pnpm - yarn)
   وقتی وارد یه پروژه میشیم نگاه میکنیم ببینیم فایل lock کدوم package manager ای وجود داره یعنی package-lock.json که متعلق به npm ، یا pnpm-lock.yaml که متعلق به pnpm ، یا yarn.lock که متعلق به yarn هست. هر کدوم بود ما هم از همون package manager استفاده می کنیم تا پروژه مرتب ادامه پیدا کنه.

یک پروژه استاندارد node.js base یعنی پروژه ای که میشه در نود جی اس اجراش کرد (نه پروژه بک اندی) همیشه دارای این ساختار هست : فایل package.json داره که داخلش dependecies رو داره پکیج های نصب شده رو نشون میده ، یه script داره که نشون میده چطوری پکیج رو run کنیم و یک lock داره که ارتباط بین پکیج ها رو نشون میده و هر موقع که پکیجی نصب می کنیم فایل هاش میره داخل node_modules.

فرض کنیم میخواهیم loadash رو نصب کنیم خب میزنیم : pnpm install loadash در این حالت پکیج loadash نصب میشه و اسمش داخل dependecies توی package.json ثبت میشه اما باید یک tradeoff ای رو در نظر بگیریم یعنی وقتی چیزی رو نصب می کنم که فقط به درد محیط development میخوره و روی پروداکشن قرار نیست ازش استفاده کنم یعنی زمانی که build سایت رو میگیرم اون پکیج کمکی به من نمیکنه و فقط در dev بهم کمک میکنه یا خطاهای من رو اصلاح میکنه یا استایل رو generate میکنه مثل تیلویند بهتره داخل devDependecies توی package.json قرار بگیره تا bundle سایت روی پروداکشن بالا نره. برای این موضوع از دستور زیر موقع نصب پکیج موردنظر استفاده می کنم:

npm i --save-dev typescript
بعنوان مثال مرورگر تایپ اسکریپت رو نمیفهمه و در پشت صحنه با واسطه با جاوااسکریپت تبدیل میشه و به همین دلیل فقط در بخش dev بهش احتایج داریم.اگر تایپ اسکریپت در dependecies هم قرار بگیره مشکلی نداره اما موقع build گرفتن ، بی دلیل حجم bundle بیشتر میشه.

یک قسمت دیگه ای هم میتونیم داخل package.json داشته باشیم به نام peerDependencies . وقتی که یک پکیج می سازیم که قراره همه ازش استفاده کنن لازمه که به اسفاده کننده ها بگیم باید فلان پکیج ها رو هم باید روی سیستمشون داشته باشن که این موضوع در peerDependencies اعلام میشه . دقت کنین قبلا گفتیم اگر لازم باشه بگیم حتما فلان پکیج رو نصب کنن اون رو داخل dependencies اعلام می کنیم.پس peerDependencies بیشتر برای کسانی هست که پکیج می نویسن. مثال عملی این موضوع : میرم داخل گیت هاب tanstack
قسمت query -> packages -> react-query ->package.json و میبینم نوشته peerDependencies : react ^18 یعنی این که لازم نیست در باندل پکیج reactquery ری اکت رو اضافه کنی اما هر کسی میخواد از این پکیج استفاده کنه باید روی پروژه اش ری اکت نسخه 18 (البته به خاطر ^ یعنی نسخه 18 با هر minor و یا patch رو میتونه داشته باشه ولی اگر بخش ماژور ورژن تغییر کنه یعنی به جای 18 نسخه 19 باشه به درد این پکیج نمیخوره) رو داشته باشه.

درباره شماره ورژن ها باید بدونیم که هر شماره ورژن از این ترکیب ساخته شده : X.Y.Z خب Z بخش Patch Version هست وقتی بالا میره که یک باگ رو فیکس کردیم ، Y بخش Minor Version است یعنی وقتی بالا میره که feature ای اضافه یا حذف کردیم و X بخش Major Version هست که با آپدیت بزرگ و اصلی و متفاوت بالا میره.

4. تفاوت package manager ها (bun - npm - pnpm - yarn)
   همه package manager های بالا از npm استفاده می کنن. در npm یک مخزن یا سرور اصلی وجود داره که همه پکیج ها روی آن قرار دارن. در yarn تعدادی سرور mirror وجود داره که به دلیل پهنای باند بالا و استراکچر بهتر و موارد دیگه برای ما در ایران بهتره و سرعت بیشتری داره و در واقع yarn میره پکیج موردنیاز کاربر رو از سرور npm میاره روی سرورهای خودش و به کاربر میده.توی pnpm روی سیستم کاربر حالت میرور رو انجام میده یا اصطلاحا کش میکنه و اگر دفعه بعد کاربر در پروژه دیگه ایی باز هم پکیج قبلی رو بخواد اول روی سیستم خود کاربر رو میگرده و اگر باشه از همون استفاده میکنه یا دقیق تر pnpm یک store مرکزی روی سیستم ایجاد می‌کند که پکیج‌ها را یک بار دانلود می‌کند و در پروژه‌های مختلف لینک می‌دهد. این باعث صرفه‌جویی چشمگیر در فضای دیسک می‌شود (نه اشغال فضای بیشتر!) و نصب را سریع‌تر می‌کند. bun تا حدی شبیه pnpm عمل میکنه اما سرعتش خیلی خوبه. همیشه فقط از یکی از این منیجرها در پروژه استفاده کنید!

5. درباره React
   برای راه اندازی هر پروژه ری اکت قبلا باید webpack و babel رو کانفیگ می کردیم.فیس بوک خودش ساختاری داشت به نام CRA = create react app که الان منسوخ شده.خب babel کمک میکنه تا جاوااسکریپت( با هر ورژن ) ای که ما می نویسیم به جاوااسکریپت قابل فهم برای مرورگر تبدیل بشه چرا جون کد جاوااسکریپتی که ما می نویسیم مدرنه (یعنی Ecmascript) و شامل chaining operator یعنی .? یا rest operator و اینطور کدها است که مرورگرهای قدیمی اینها رو نمی فهمن پس لازمه به کمک babel تبدیل بشه.Webpack یک باندلر است و همه چیز رو در chunk های مختلف بسته بندی میکنه یعنی مثل یک کارخانه بسته‌بندی است که همه فایل‌های پروژه (JS, CSS, تصاویر، فونت‌ها و...) رو می‌گیره ، وابستگی‌ها رو تحلیل می‌کنه (کدام فایل به کدام فایل نیاز داره) ، بهینه‌سازی می‌کنه (حذف کدهای اضافی، فشرده‌سازی و...) و یک یا چند فایل نهایی تولید می‌کنه که مستقیماً در مرورگر قابل استفاده‌اند یا مثلا اگر کد SASS یا LESS داشته باشیم webpack به css تبدیل میکنه. خب حالا به جای CRA از Vite استفاده می کنیم.

یه راه متفاوت برای باز کردن vscode : برو به مسیر پوشه پروژه آدرس رو در آدرس بار پاک کن بزن cmd و enter بعد داخل cmd بزن (. code) اینطوری vscode پروژه باز میشه 😊

6. درباره CRA
   وقتی چیزی رو با npm یا بقیه پکیج منیجرها نصب می کنیم میره یک node package module نصب میکنه یعنی یه پکیج از یه سری کد آماده که دولوپر دیگه ایی نوشته رو نصب میکنه اما یک کامند دیگه ای داریم به نام npx که کارش run کردن bash script هست. bash script یه فایله که داخلش یه اسکریپت یا یه کد نوشته شده که میتونه مرحله به مرحله با سیستم عامل شما ارتباط بگیره فایل بسازه ازتون سوال بپرسه که چی رو نصب کنه در کل پکیج نیست بلکه یه سری اسکریپته که روی سیستم شما اجرا میشه و چیزهایی میسازه و نصب میکنه.

خب با دستور زیر CRA رو برای ما در همین دایرکتوری (با گذاشتن .) نصب میکنه :

npx create-react-app .
یا با دستورات زیر با توجه به پکیج منیجر مورد استفاده :

pnpm create react-app .
yarn create react-app .
bun create react-app .
خب ما از pnpm استفاده می کنیم و نصب می کنیم.

7. درباره Vite
   درواقع Vite یک build frontend tool است ، Vite ِنسبت به CRA ، تجربه توسعه (DX Develop Exprience) بهتر و سرعت بالاتری داره.

pnpm create vite
در ری اکت دو تا پسوند برای فایل ها داریم js و jsx اما در جاوااسکریپت اینها هیچ تفاوتی ندارن البته بعضی ها میگن چون ماهیت ری اکتی داره بهتره از jsx استفاده کنیم . JSX یک شکل نوشتاری راحت‌تر برای نوشتن UI در React است که قبل از اجرا به JavaScript معمولی تبدیل میشه.

مورد JS JSX
زبان JavaScript خالص سینتکس روی JS
شبیه HTML ❌ ✅
نیاز به Transpile ❌ ✅ (Babel)
خوانایی در UI کمتر خیلی بیشتر
قابل اجرا مستقیم در مرورگر ✅ ❌
اما این موضوع در تایپ اسکریپت متفاوته ، اگر پسوند tsx استفاده کنیم یعنی داخل فایل از jsx استفاده کردیم ولی اگر پسوند ts دادیم حق نوشتن jsx درون آن فایل را نداریم.

8. درباره Component Composition
   ء Component Composition یکی از مهم‌ترین اصول طراحی در React ـه. ایدهٔ اصلیش خیلی ساده‌ست:

به‌جای ساخت کامپوننت‌های غول‌پیکر یا استفاده از inheritance، کامپوننت‌های کوچک رو کنار هم می‌چینیم.

-ء Composition یعنی چی؟

یعنی:

کامپوننت‌های کوچک، مستقل و قابل‌استفاده‌مجدد
که با هم ترکیب می‌شن و UI بزرگ‌تر رو می‌سازن
اصول مهم Composition Component در React
1️⃣ء Single Responsibility

هر کامپوننت فقط یک مسئولیت داشته باشد.

❌ بد:

<UserPage /> // هم دیتا می‌گیره، هم فرم داره، هم لیست، هم استایل
✅ خوب:

<UserPage>
  <UserProfile />
  <UserPosts />
</UserPage>
2️⃣ء Composition به‌جای Inheritance

در React تقریباً ارث‌بری نداریم.

❌ بد:

class AdminButton extends Button {}
✅ خوب:

<Button variant="admin" />
3️⃣ استفاده از children

قوی‌ترین ابزار Composition.

function Card({ children }) {
return <div className="card">{children}</div>;
}
استفاده:

<Card>
  <h2>Title</h2>
  <p>Description</p>
</Card>
4️⃣ء Prop-based Composition

رفتار و ظاهر از بیرون کنترل شود.

<Button size="lg" variant="primary" disabled />
به‌جای:

<LargePrimaryDisabledButton />
5️⃣ء Container / Presentational Separation

جدا کردن:

منطق (data, state)
از UI (render)
<UserContainer>
<UserView />
</UserContainer>
یا با hook:

const user = useUser();
<UserView user={user} />
6️⃣ء Slot Pattern (Named Children)

وقتی چند جای قابل‌جایگزینی داریم:

<Modal
header={<Header />}
footer={<Footer />}

>   <Content />
> </Modal>
> یا:

<Layout>
  <Layout.Header />
  <Layout.Content />
</Layout>
7️⃣ء Compound Components

کامپوننت‌های مرتبط که با هم کار می‌کنند.

<Tabs>
  <Tabs.List>
    <Tabs.Trigger />
  </Tabs.List>
  <Tabs.Panel />
</Tabs>
مزیت:

ء API تمیز
استفاده منعطف
8️⃣ء Render Props (در صورت نیاز)

کنترل کامل روی رندر:

<List
items={data}
render={(item) => <Item data={item} />}
/>
9️⃣ ترجیح Composition به Prop Explosion

وقتی prop زیاد می‌شود، composition بهتر است.

❌ بد:

<Modal
  showHeader
  showFooter
  showClose
  headerTitle="..."
/>
✅ خوب:

<Modal>
  <Modal.Header />
  <Modal.Body />
  <Modal.Footer />
</Modal>
نشانه‌های Composition خوب
✅ کامپوننت‌ها کوچک‌اند ✅ قابل تست‌اند ✅ قابل reuse هستند ✅ تغییر یک بخش بقیه را نمی‌شکند ✅ خوانایی API

خلاصهٔ یک‌خطی
ء Composition یعنی ساخت UI با کنار هم گذاشتن کامپوننت‌های کوچک و مستقل، نه ارث‌بری یا کامپوننت‌های غول‌پیکر.

9. درباره Component
   در ری اکت همه چیز کامپوننت است. دو تا کامپوننت اصلی توی ری اکت داریم : main.jsx و App.jsx. وظیفه main.jsx معرفی app.jsx به ری اکت جهت رندر شدن است و همه اتفاقات داخل app.jsx میفته. این ساختار مثل دیتاتایپ tree است که دربالای همه app.jsx قرار گرفته. قبلا در جاوااسکریپت دیدیم که DOM یک ساختار tree است که JS میتونه با استفاده از اون element های html رو پیدا کنه و روابط بینشون رو کشف کنه و در DOM همه چیز به html که در بالای این tree قرار گرفته برمیگرده. app.jsx هم همین طور است و در بالای tree قرار گرفته. app.jsx اجاازه تعامل با engine ری اکت رو داره.

10. درباره Class Component و Functional Component
    2 تا روش برای ساخت کامپوننت داریم. تا ری اکت 10.0.8 class comp ها استفاده میشدن اما الان دیگه از functional comp ها استفاده میشه. CC ها Stateful function بودن یعنی حافظه ای برای ذخیره state داشتن اما FC ها Stateless function بودن و حافظه ای نداشتن. بعد از ری اکت 16.0.8 هوک ها معرفی شدن و استفاده از CC به پایان رسید.

11. درباره JSX
    اصلا JSX چیست و چه کاربردی دارد؟

ءJSX که مخفف JavaScript XML است، در واقع یک سینتکس اکستنشن (Syntax Extension) برای زبان جاوا اسکریپت محسوب می‌شود. هدف اصلی JSX این است که به توسعه‌دهنده اجازه دهد با استفاده از دانشی که از HTML دارد، کدهای خود را بنویسد، اما در نهایت این کدها توسط جاوا اسکریپت به چیزی تبدیل می‌شوند که برای پلتفرم مقصد (مانند مرورگر) قابل فهم باشد. اگرچه ظاهر JSX شبیه به HTML است، اما در واقع این‌طور نیست و پس از کامپایل شدن، به اشیاء جاوا اسکریپتی تبدیل می‌شود که ری‌اکت آن‌ها را درک می‌کند.

سازوکار داخلی و تکامل JSX

در نسخه‌های قدیمی‌تر ری‌اکت (قبل از نسخه ۱۷)، هر قطعه کد JSX که نوشته می‌شد، در هنگام کامپایل به دستور React.createElement تبدیل می‌شد . به همین دلیل، توسعه‌دهندگان مجبور بودند در بالای تمامی فایل‌های کامپوننت خود، کتابخانه ری‌اکت را ایمپورت کنند، حتی اگر مستقیماً از کلمه "React" در کد استفاده نمی‌کردند.

از نسخه ۱۷ به بعد، این الزام برداشته شد؛ زیرا JSX اکنون به فرمت پیچیده‌تری تبدیل می‌شود که ری‌اکت خودش آن را مدیریت و ایمپورت می‌کند . این فرآیند تغییر کد از یک حالت جاوا اسکریپت به حالت دیگر، اصطلاحاً ترنسپایل (Transpile) نامیده می‌شود. محبوبیت اصلی ری‌اکت نیز از زمانی آغاز شد که JSX معرفی گشت، زیرا کار با آن بسیار ساده‌تر از نوشتن دستیِ بی‌شمار دستور React.createElement برای المنت‌های تودرتو است.

بهبود تجربه توسعه‌دهنده (DX) و تفاوت با HTML

ری‌اکت با استفاده از JSX، سعی کرده است تجربه توسعه‌دهنده (Developer Experience) را بهبود ببخشد. به جای اینکه کاربر مجبور باشد سینتکس‌های پیچیده ری‌اکت را یاد بگیرد، از همان ساختار مشابه HTML استفاده می‌کند . با این حال، تفاوت‌های کلیدی وجود دارد :

اتریبیوت‌های اختصاصی: بسیاری از ویژگی‌های HTML در JSX تغییر نام داده‌اند؛ برای مثال به جای class از className استفاده می‌شود و رویدادهایی مثل onclick به صورت onClick (با حروف بزرگ/CamelCase) نوشته می‌شوند.
شبیه‌سازی ویژگی‌ها: ری‌اکت تمامی المنت‌های HTML (مانند div ،button ،h1 و...) و ویژگی‌های آن‌ها را به صورت دستی در خود تعریف و همانندسازی کرده است . حتی ویژگی‌هایی مانند aria-label یا id که در اکثر المنت‌ها مشترک هستند، با استفاده از جنریک‌های تایپ‌اسکریپت در ساختار ری‌اکت گنجانده شده‌اند .
کلمات رزرو شده: برخی کلمات در جاوا اسکریپت رزرو شده هستند و نمی‌توان از آن‌ها مستقیماً به عنوان نام ویژگی استفاده کرد، به همین دلیل ری‌اکت معادل‌های خاص خود را برای آن‌ها در نظر گرفته است.
چرا از JSX استفاده می‌کنیم؟

نوشتن کدها به صورت مستقیم با React.createElement کاری بسیار دشوار و غیرمنطقی است، به خصوص زمانی که تعداد المنت‌ها زیاد و تودرتو می‌شود. استفاده از JSX نه تنها کد را تمیزتر و خواناتر می‌کند، بلکه سرعت توسعه را نیز به شدت افزایش می‌دهد.

در نهایت، باید دانست که در ری‌اکت هیچ چیزی واقعاً HTML نیست؛ بلکه همه چیز جاوا اسکریپت است که با ایده‌های HTML همانندسازی شده تا توسعه‌دهندگان بتوانند راحت‌تر با آن ارتباط برقرار کنند. این ساختار در نهایت به ری‌اکت کمک می‌کند تا المنت‌ها را در Virtual DOM مقایسه کرده و تغییرات لازم را در صفحه اعمال کند. درک عمیق این مفاهیم زیرساختی، تفاوت میان یک توسعه‌دهنده معمولی و یک متخصص فرانت‌اند را رقم می‌زند.

مثالی برای درک بهتر:

کار با JSX مانند استفاده از یک دستگاه ترجمه همزمان است. شما به زبان مادری خود (HTML) صحبت می‌کنید، اما دستگاه (کامپایلر ری‌اکت) گفته‌های شما را به زبانی تبدیل می‌کند که برای شنونده (مرورگر) کاملاً دقیق و تخصصی است، بدون اینکه شما نیاز داشته باشید قواعد پیچیده زبان مقصد را مستقیماً به کار ببرید.

12. درباره Hot Reload Module
    هر تغییری که در کدهای ری اکت ایجاد می کنیم بلافاصله و بدون آن که صفحه رو رفرش کنیم در خروجی پورت 5173 شاهدش هستیم به این مساله Hot Reload Module میگیم. خب مرورگر چطوری میفهمه؟ میریم به کنسول مرورگر تب Network قسمت Socket : در این قسمت می بینیم که یه وب سوکت به نام token وجود داره ( که داخلش قسمت Messages آپدیت ها رو نشون میده و آپدیت ها از طریق event بهش میرسه) که مرورگر از طریق همین وب سوکت متوجه تغییرات میشه. در واقع Webpack میاد یه websocket درست میکنه که هر تغییری رو از طریق وب سوکت به مرورگر اطلاع بده. این مساله فقط در محیط developement اتفاق میفته. و در محیط production اصلا این وب سوکت رو نداریم. توجه کنین که HRM متعلق به webpack و turbopack و به طور کلی متعلق به این bundler ها است.

13. درباره Props
    پراپس که مخفف کلمه Properties (ویژگی‌ها) است، به دیتایی گفته می‌شود که کامپوننت‌ها می‌توانند از طریق آن با یکدیگر تبادل اطلاعات کنند. در واقع پراپس راهی برای برقراری ارتباط بین کامپوننت والد (Parent) و فرزند (Child) است.

تفاوت اصلی استیت و پراپس

هر کامپوننت در ری‌اکت می‌تواند یک حافظه داخلی ایزوله داشته باشد که به آن استیت (State) می‌گوییم؛ حتی اگر از یک کامپوننت چندین بار استفاده کنیم، هر کدام استیت مخصوص به خود را دارند و تغییر یکی روی دیگری اثر نمی‌گذارد. اما پراپس دیتایی است که از بیرون به کامپوننت داده می‌شود تا ظاهر یا رفتار آن را تغییر دهد .

مکانیزم Re-render شدن در اثر تغییر پراپس

یکی از سوالات مهم در مصاحبه‌های برنامه‌نویسی این است که چه چیزهایی باعث ریرندر شدن کامپوننت می‌شود؟ پاسخ رایج «تغییر استیت» و «تغییر پراپس» است، اما منابع آموزشی نکته دقیق‌تری را مطرح می‌کنند:

۱. علت اصلی، تغییر استیت والد است: اینکه می‌گویند تغییر پراپس باعث ریرندر می‌شود، از نظر لغوی کمی دقیق نیست در واقع، پراپسی که به یک کامپوننت فرزند پاس داده شده، معمولاً خودش یک استیت در کامپوننت والد بوده است. وقتی آن استیت در والد تغییر می‌کند، کل کامپوننت والد ریرندر می‌شود.

۲. فرآیند ریرندر زنجیره‌ای: از آنجایی که کامپوننت فرزند بخشی از خروجی (Return) کامپوننت والد است، وقتی والد از نو فراخوانی (Call) می‌شود، فرزند هم به اجبار دوباره فراخوانی و ریرندر می‌گردد . پس در واقع، تغییر استیت والد باعث نوسازی خودش و تمام فرزندانش می‌شود.

۳. جلوگیری از ریرندرهای اضافی با Memo: اگر بخواهیم جلوی ریرندر شدن بی مورد فرزند (زمانی که والد تغییر کرده اما دیتای فرزند تغییری نداشته) را بگیریم، از قابلیتی به نام Memo استفاده می‌کنیم . با ممو کردن، به کامپوننت می‌گوییم فقط در صورتی اجازه ریرندر دارد که پراپس‌های خودش واقعاً تغییر کرده باشند، نه صرفاً به خاطر ریرندر شدن والد . این کار باعث افزایش پرفورمنس (Performance) و کاهش فشار روی CPU و رم دستگاه کاربر می‌شود.

ویژگی‌ها و نحوه استفاده از پراپس

مشابه اتریبیوت‌های HTML: پراپس‌ها بسیار شبیه به ویژگی‌هایی مثل href یا src در تگ‌های HTML هستند .
ورودی فانکشن: از نظر فنی، پراپس چیزی نیست جز ورودی یک تابع . ری‌اکت تمام ویژگی‌هایی که به کامپوننت می‌دهید را در قالب یک آبجکت جمع کرده و به عنوان ورودی به تابع کامپوننت پاس می‌دهد
قرارداد نام‌گذاری: برای نوشتن پراپس‌ها باید از استاندارد camelCase استفاده کرد (مثلاً isActive به جای isactive)
انواع دیتا: شما می‌توانید هر نوع دیتایی را به عنوان پراپس پاس دهید؛ از انواع اولیه (عدد، استرینگ) گرفته تا توابع و آبجکت‌های پیچیده .
نکته: برای پاس دادن استرینگ‌های ثابت نیازی به کرلی بریس {} نیست، اما برای کدهای جاوااسکریپتی یا استرینگ‌های داینامیک حتماً باید از {} استفاده کرد
چرا استفاده اصولی از پراپس مهم است؟

استفاده درست از پراپس به ما کمک می‌کند تا کامپوننت‌های Reusable (با قابلیت استفاده مجدد) بسازیم.

اصل Open-Closed: یک کامپوننت خوب باید برای توسعه (Extension) باز و برای تغییر(Modification) بسته باشد. پراپس به ما اجازه می‌دهد بدون دست بردن در کدهای داخلی کامپوننت، ظاهر و رفتار آن را از بیرون کنترل کنیم.
کاهش وابستگی: اگر کامپوننتی را بیش از حد به استیت‌های داخلی یا دیتای پروژه‌ای خاص وابسته کنید، نمی‌توانید آن را در پروژه‌های دیگر استفاده کنید. اما با استفاده از پراپس، کامپوننت مستقل‌تر شده و طبق اصول DRY (تکرار نکردن کد) و KISS (ساده نگه داشتن کد) طراحی می‌شود.
به عنوان یک تمثیل:

پراپس مانند بسته‌ای است که دمِ درِ خانه (کامپوننت) به شما تحویل می‌دهند . شما به عنوان صاحب‌خانه (کامپوننت)، بسته را باز می‌کنید و بر اساس محتویات آن (دیتا یا دستورالعمل‌ها)، تصمیم می‌گیرید که چه واکنشی نشان دهید یا دکوراسیون خانه را چگونه تغییر دهید. این محتویات از بیرونِ خانه کنترل می‌شوند، اما تصمیم‌گیرنده نهایی برای نحوه نمایش، خودِ کامپوننت است.

14. رندرینگ لیست‌ها، اهمیت key و مکانیزم رندر مجدد (Re-render)
    نمایش لیست‌ها و اهمیت هویت در ری‌اکت

در برنامه‌نویسی ری‌اکت، بسیار پیش می‌آید که بخواهیم مجموعه‌ای از داده‌ها (مثلاً لیستی از نام افراد یا تسک‌ها) را که از یک دیتابیس یا آرایه می‌آیند، در صفحه نمایش دهیم. برای این کار، به جای کدنویسی دستی تک‌تک آیتم‌ها (Hard-coding)، از متد map استفاده می‌کنیم. متد map روی تک‌تک اعضای آرایه پیمایش کرده و برای هر کدام، یک المنت (مثل تگ li) تولید می‌کند.

تفاوت اصلی map با متدی مثل forEach در این است که map یک خروجی (Return) دارد که ری‌اکت می‌تواند آن را مستقیماً در خروجی کامپوننت رندر کند، در حالی که forEach صرفاً یک عملیات را انجام می‌دهد و چیزی برنمی‌گرداند.

چرا ری‌اکت به Key نیاز دارد؟

زمانی که ما مجموعه‌ای از المان‌ها را به صورت پویا تولید می‌کنیم، ری‌اکت با یک چالش مواجه می‌شود: این المان‌ها برای او شبیه به «جعبه‌های بی‌نام و نشانی» هستند که هویتی ندارند. اگر یکی از این آیتم‌ها تغییر کند، حذف شود یا آیتم جدیدی به لیست اضافه شود، ری‌اکت بدون داشتن یک هویت مشخص برای هر المان، نمی‌تواند تشخیص دهد دقیقاً کدام بخش از لیست تغییر کرده است.

در چنین حالتی، ری‌اکت ممکن است مجبور شود کل لیست را از اول رندر کند که این کار باعث کاهش پرفورمنس و افزایش فشار روی منابع سیستم می‌شود. ویژگی key در واقع حکم «کارت شناسایی» یا «اسم» هر المان را دارد. با دادن یک کلید منحصربه‌فرد به هر آیتم، به ری‌اکت می‌فهمانیم که هر المان دقیقاً کجای لیست قرار دارد.

مکانیزم Re-render و مدیریت استیت

تغییر در رابط کاربری زمانی اتفاق می‌افتد که استیت (State) یا وضعیت داخلی کامپوننت تغییر کند. وقتی ما لیستی داریم که در استیت ذخیره شده، برای اضافه کردن یا حذف کردن یک عضو، باید طبق قوانین ری‌اکت عمل کنیم:

۱. کپی گرفتن از استیت (Shallow Copy): از آنجایی که آرایه‌ها در جاوااسکریپت به صورت Reference (ارجاعی) هستند، ما نباید استیت اصلی را مستقیماً تغییر دهیم (Mutate نکنیم). ابتدا باید با استفاده از ابزارهایی مثل Spread Operator (...) یک کپی از آرایه تهیه کنیم، تغییرات را روی نسخه کپی اعمال کرده و سپس با تابع setState آن را به‌روزرسانی کنیم. این کار باعث می‌شود ری‌اکت متوجه تغییر رفرنس شده و دستور رندر مجدد را صادر کند.

۲. رندر هوشمند با استفاده از Key: وقتی استیت تغییر می‌کند و کامپوننت دوباره فراخوانی می‌شود، ری‌اکت به کلیدهای (Keys) لیست نگاه می‌کند. اگر ببیند که کلیدهای قدیمی همچنان سر جایشان هستند و فقط یک کلید جدید اضافه شده، فقط همان یک المان جدید را به دام (DOM) اضافه می‌کند و بقیه لیست را بدون تغییر باقی می‌گذارد. این موضوع از رندرهای بی مورد و سنگین جلوگیری می‌کند.

اشتباهات رایج در انتخاب Key

استفاده از Index آرایه: بسیاری از برنامه‌نویسان از ایندکس (شماره ردیف) آرایه به عنوان کلید استفاده می‌کنند. این کار به شدت نهی شده است؛ زیرا اگر شما یک آیتم را از وسط لیست حذف کنید یا ترتیب لیست را تغییر دهید، ایندکس‌های تمام آیتم‌های بعدی عوض می‌شود. با تغییر ایندکس، ری‌اکت تصور می‌کند تمام آن المان‌ها تغییر کرده‌اند و دوباره آن‌ها را رندر می‌کند که عملاً خاصیت Key را از بین می‌برد.
استفاده از مقادیر ناپایدار: نباید از مقادیری مثل ساعتِ لحظه‌ای یا اعداد تصادفی (Random) در زمان رندر به عنوان کلید استفاده کرد، چون در هر بار رندر، این مقدار عوض شده و ری‌اکت دوباره مجبور به ساختن کل المان از نو می‌شود.
نتیجه‌گیری: برای داشتن یک اپلیکیشن بهینه، همیشه باید از مقادیر ثابت و منحصربه‌فرد (مثل IDهای دیتابیس) برای ویژگی key استفاده کنیم. این کار به ری‌اکت اجازه می‌دهد تا به جای تخریب و بازسازی کل لیست، فقط تغییرات جزئی را در صفحه اعمال کند که این امر کلید اصلی سرعت و کارایی در پروژه‌های بزرگ است.

15. درباره Conditional Rendering
    کاندیشنال رندرینگ به زبان ساده یعنی اینکه ما بر اساس یک سری قواعد، قوانین و شروط، تصمیم بگیریم که یک جزء (Component) یا بخشی از رابط کاربری را نمایش دهیم یا نمایش ندهیم. این موضوع یکی از اساسی‌ترین مفاهیم در توسعه فرانت‌اند است که به ما اجازه می‌دهد اپلیکیشن‌های پویا بسازیم.

پیش‌نیاز: درک مقادیر Truthy و Falsy

برای تسلط بر رندرینگ شرطی، ابتدا باید بدانیم جاوا اسکریپت چه مقادیری را «نادرست» یا Falsy در نظر می‌گیرد. این مقادیر شامل موارد زیر هستند:

عدد صفر (0) و منفی صفر (-0)
استرینگ خالی ("")
مقدار null و undefined
مقدار NaN (Not a Number)
و خودِ کلمه false
هر چیزی غیر از این موارد، در جاوا اسکریپت Truthy یا «درست» تلقی می‌شود. در ری‌اکت، وقتی شرطی را داخل خروجی کامپوننت می‌نویسیم، موتور جاوا اسکریپت به صورت خودکار این مقادیر را به نوع بولین (True/False) تبدیل می‌کند تا تکلیف نمایش یا عدم نمایش مشخص شود.

روش‌های اجرای رندرینگ شرطی در ری‌اکت

۱. استفاده از ترنری آپریتور (Ternary Operator):

رایج‌ترین و تمیزترین راه برای شرطی‌سازی در ری‌اکت، استفاده از عملگر سه تایی است. به جای نوشتن چندین خط کد if و else که باعث شلوغی می‌شود، می‌توانیم در یک خط بنویسیم: «اگر شرط برقرار بود، این را نشان بده، در غیر این صورت آن را نشان بده». این روش به دلیل خلاصه بودن و قابلیت استفاده در داخل کدهای JSX (در میان براکت‌های {}) بسیار محبوب است.

۲. استفاده از منطق کوتاه (Short Circuit):

گاهی اوقات می‌خواهیم بگوییم «فقط اگر شرط برقرار بود، این مورد را نشان بده و اگر نبود، اصلاً چیزی نشان نده». در این حالت از علامت && استفاده می‌کنیم. در این ساختار، اگر سمت چپ شرط درست باشد، ری‌اکت مستقیماً بخش سمت راست را رندر می‌کند.

۳. استفاده از بازگشت‌های متعدد (Multiple Returns):

یک روش دیگر این است که در بدنه اصلی تابع کامپوننت، از دستور if استفاده کنیم و بر اساس آن، کل خروجی کامپوننت را تغییر دهیم. اگرچه این روش کار می‌کند، اما معمولاً برای کدهای طولانی و پیچیده، خوانایی کمتری نسبت به روش ترنری دارد.

مدیریت وضعیت با یک مثال کاربردی (Toggle)

یکی از بهترین مثال‌ها برای درک این مفهوم، ساخت یک دکمه «تاتگل» است. فرض کنید یک استیت به نام isActive داریم که مقدار اولیه آن false است. ما می‌توانیم تابعی بنویسیم که با هر بار کلیک بر روی دکمه، مقدار این استیت را برعکس کند (اگر true بود false شود و برعکس). سپس در بخش نمایش، با استفاده از رندرینگ شرطی مشخص می‌کنیم که اگر وضعیت فعال بود متن "نمایش" و در غیر این صورت متن "عدم نمایش" چاپ شود.

نکته بسیار مهم و باگ احتمالی: عدد صفر

یک نکته کلیدی که بسیاری از برنامه‌نویسان را دچار چالش می‌کند، رفتار عدد صفر در رندرینگ شرطی است. از آنجایی که صفر یک مقدار Falsy است، اگر شما در شرط خود از && استفاده کنید و مقدار متغیر شما صفر باشد، ری‌اکت ممکن است به جای نمایش ندادن چیزی، عدد صفر را در ظاهر سایت به کاربر نشان دهد. این اتفاق به این دلیل می‌افتد که جاوا اسکریپت صفر را به عنوان نتیجه نهایی شرط برمی‌گرداند. بنابراین همیشه باید حواستان باشد که اگر با اعداد کار می‌کنید، شرط را به گونه‌ای بنویسید که وجود یا عدم وجود عدد را به درستی کنترل کند تا زنجیره رندرینگ شما قطع نشود یا کاراکترهای اضافی در صفحه ظاهر نشود.

در نهایت، رندرینگ شرطی ابزاری قدرتمند است که به شما اجازه می‌دهد تجربه کاربری را بر اساس وضعیت‌های مختلف (مثل لاگین بودن کاربر، بارگذاری داده‌ها یا خطاهای احتمالی) به بهترین شکل مدیریت کنید.

16. درباره هوک ها
    ورود به دنیای هوک‌ها (React Hooks)

از نسخه ۱۶ ری‌اکت به بعد، مفهومی به نام «هوک» معرفی شد که انقلابی در توسعه وب ایجاد کرد. هوک‌ها در واقع قلاب‌هایی هستند که به ما اجازه می‌دهند تمام قابلیت‌هایی که پیش از این فقط در «کلاس کامپوننت‌ها» وجود داشت (مانند مدیریت وضعیت و چرخه حیات)، به «فانکشنال کامپوننت‌ها» اضافه کنیم. این کار باعث ساده‌تر شدن کدنویسی و افزایش خوانایی پروژه‌ها شده است.

هوک useRef: حافظه پنهان و دسترسی به DOM

یکی از هوک‌های بسیار کاربردی، useRef است که دو وظیفه اصلی بر عهده دارد:

۱. ذخیره‌سازی دیتا بدون ریرندر: تفاوت اصلی این هوک با useState در این است که اگر شما مقداری را در یک (Ref) تغییر دهید، کامپوننت شما متوجه این تغییر نمی‌شود و صفحه دوباره رندر (Re-render) نمی‌گردد. این ویژگی برای ذخیره اطلاعاتی که نیاز به نمایش مستقیم در ظاهر سایت ندارند اما باید در طول عمر کامپوننت ثابت بمانند، بسیار عالی است. در واقع Ref از تمام تغییرات ناشی از رندر مجدد مصون می‌ماند.

۲. دسترسی به المان‌های صفحه: در جاوااسکریپت خالص، ما برای انتخاب یک المان از دستوراتی مثل document.querySelector استفاده می‌کردیم. اما در ری‌اکت، ما حق نداریم مستقیماً در DOM دست ببریم. به جای آن، از useRef استفاده می‌کنیم تا رفرنس (مرجع) یک المان مثل یک دکمه، اینپوت یا تگ ویدیو را بگیریم. برای مثال، در ساخت یک موزیک‌پلیر، برنامه‌نویسان رفرنس تگ آدیو را می‌گیرند تا بتوانند با کدهای جاوااسکریپتی دستور پخش (Play) یا توقف (Pause) را روی آن اجرا کنند.

هوک useEffect: مدیریت ساید‌افکت‌ها

هوک useEffect برای مدیریت اتفاقاتی استفاده می‌شود که در خارج از حیطه اصلی رندرینگ رخ می‌دهند؛ مواردی مثل دریافت اطلاعات از سرور (API Call)، تنظیم تایمرها یا واکنش به تغییرات یک استیت خاص.

این هوک یک تابع callback و یک «آرایه وابستگی» (Dependency Array) دریافت می‌کند که رفتار آن را تعیین می‌کند:

حالت بدون آرایه: اگر هیچ آرایه‌ای ندهید، هوک با هر بار رندر شدن کامپوننت اجرا می‌شود که معمولاً خطرناک است و می‌تواند باعث کاهش سرعت شود.
آرایه خالی []: در این حالت، کدهای داخل هوک فقط و فقط یک بار (زمانی که کامپوننت برای اولین بار در صفحه ظاهر یا Mount می‌شود) اجرا می‌گردد. این معادل دقیق componentDidMount در کلاس‌ کامپوننت هاست (در مصاحبه سوال میشه معمولا) و بهترین جا برای گرفتن دیتا از سرور در ابتدای لود شدن صفحه است.
آرایه با وابستگی مشخص [state]: در این حالت، هوک گوش به زنگ می‌ماند تا هر زمان که آن استیت خاص تغییر کرد، کدهای داخل خود را دوباره اجرا کند. این یک راه عالی برای واکنش نشان دادن به تغییرات دیتا است.
نکات حیاتی و هشدارهای امنیتی

استفاده نادرست از useEffect می‌تواند منجر به باگ‌های بزرگی شود. برای مثال، اگر در آرایه وابستگی از آبجکت‌ها یا آرایه‌ها استفاده کنید، باید بدانید که ری‌اکت این‌ها را از روی «رفرنس» یا آدرس حافظه چک می‌کند. بنابراین حتی اگر ظاهر آبجکت یکی باشد، ری‌اکت ممکن است فکر کند تغییر کرده و مدام کد را اجرا کند.

همچنین باید بسیار مراقب بود که یوزافکت باعث ایجاد حلقه‌های بی‌پایان (Infinite Loops) نشود؛ یعنی کدی ننویسید که خودش باعث تغییر استیتی شود که خودش به آن وابسته است. این اتفاق می‌تواند به قدری سنگین باشد که حتی سرویس‌های بزرگی مثل کلاودفلر را هم با چالش‌های جدی روبرو کند.

یادگیری پروژه محور: اصل اساسی

در نهایت، باید به این نکته توجه داشت که یادگیری تئوریک این هوک‌ها به تنهایی کافی نیست. مفاهیمی مثل هوک‌های سفارشی (Custom Hooks)، مدیریت فرم‌ها با ابزارهایی مثل React Hook Form و ارتباط با سرور با React Query، زمانی معنا پیدا می‌کنند که در دل یک پروژه واقعی به کار گرفته شوند. در پروژه‌های واقعی است که برنامه‌نویس درک می‌کند چه زمانی باید یک دیتا را در استیت نگه دارد تا صفحه ریرندر شود و چه زمانی از Ref استفاده کند تا پرفورمنس اپلیکیشن حفظ گردد.

به زبان ساده، هوک‌ها ابزارهایی هستند که به کامپوننت‌های ما هوشمندی و توانایی تعامل با دنیای بیرون را می‌دهند، به شرطی که بدانیم هر کدام در چه فازی از چرخه حیات کامپوننت باید وارد عمل شوند.

17. درباره شروع یک پروژه و همکاری در تیم
    در این بخش از دوره جامع فرانت‌اند پیشرفته، وارد فاز جدیدی به نام پروژه ژلاتو هاوس (Gelato House) می‌شویم. هدف این است که فرآیند ساخت یک پروژه را از صفرِ صفر، دقیقاً به همان صورتی که در دنیای واقعی و حرفه‌ای انجام می‌شود، با هم پیش ببریم.

رویکرد یادگیری در شروع مسیر

برای کسانی که در ابتدای راه هستند، بسیار مهم است که در چند سال اول کار (حدود ۳ تا ۴ سال اول)، از کدهای دیگران و نحوه تفکر برنامه‌نویسان با‌تجربه الهام بگیرند (Inspiration) و حتی سبک کاری آن‌ها را کپی کنند. این یک روند طبیعی است تا زمانی که به تجربه کافی برسید و بتوانید دست به نوآوری و ابداع (Innovation) بزنید. در این دوره، من قدم‌به‌قدم نحوه تفکر و استراتژی‌های اجرایی خود را به شما منتقل می‌کنم.

ساختار یک تیم استارتاپی حرفه‌ای

تصور کنید شما به عنوان یک فرانت‌اند دولپر عضو یک تیم شده‌اید. در یک تیم استاندارد، نقش‌های کلیدی زیر وجود دارند که همگی مکمل یکدیگرند:

مدیر محصول (Product Manager - PM): وظیفه او ترجمه نیازهای بیزینس به زبان فنی و تسک‌های اجرایی است. او مدیریت کل محصول، از نظر فانکشنالیتی و زمان‌بندی را بر عهده دارد.
مدیر فنی (CTO): شخصی که کل تیم فنی را مدیریت و هدایت می‌کند.
تیم توسعه (Development Team): شامل یک یا چند برنامه‌نویس بک‌اند (Backend) و فرانت‌اند (Frontend) است.
اسکرام مستر (Scrum Master): در تیم‌هایی که از متدولوژی اجایل (Agile) و اسکرام (Scrum) استفاده می‌کنند، این شخص تسک‌ها را در بازه‌های زمانی معمولاً دو هفته‌ای به نام اسپرینت (Sprint) می‌چیند و بر اساس ظرفیت هر عضو، وظایف را اختصاص می‌دهد.
طراح محصول (Product Designer): کسی که طرح‌های گرافیکی و تجربه کاربری اپلیکیشن را آماده می‌کند. ما در این پروژه از ابزار فیگما (Figma) استفاده می‌کنیم که طرح‌های دسکتاپ و موبایل ژلاتو هاوس در آن آماده شده است.
اهمیت طراحی و یک نکته حیاتی

یک قانون مهم برای هر برنامه‌نویس فرانت‌اند این است: هرگز پروژه‌ای را که دیزاین مشخص ندارد، قبول نکنید. پروژه‌هایی که دیزاین ندارند و بر اساس سلیقه شخصی پیش می‌روند، معمولاً با شکست یا تغییرات بی‌پایان مواجه می‌شوند. داشتن یک طرح تایید شده در فیگما، مرز بین کار حرفه‌ای و غیرحرفه‌ای است.

مسیر فنی پروژه ژلاتو هاوس

این پروژه در دو فاز اصلی اجرا خواهد شد: ۱. توسعه با کتابخانه React: ابتدا کل پروژه را با ریاکت می‌سازیم و در حین کار، نکات پیشرفته و چالش‌های آن را بررسی می‌کنیم. ۲. انتقال به Next.js: پس از اتمام پروژه با ریاکت، در یک فصل مجزا استک پروژه را به نکست‌جی‌اس تغییر می‌دهیم. هدف این است که یاد بگیرید چگونه یک پروژه ریاکتی را به راحتی به قالب نکست‌جی‌اس ببرید و از مزایای آن بهره‌مند شوید.

فلسفه یادگیری پروژه محور

واقعیت دنیای برنامه‌نویسی این است که شما هر چقدر هم تئوری بخوانید، تا زمانی که دست به کد نشوید و پروژه واقعی انجام ندهید، موضوعات را عمیقاً درک نخواهید کرد. پروژه است که شما را در دنیای فرانت‌اند و بک‌اند نجات می‌دهد.

ما در این دوره، حضور در یک تیم حرفه‌ای را شبیه‌سازی می‌کنیم و من تمام تجربیات و نکاتی را که در پروژه‌های واقعی آموخته‌ام، بدون هیچ کم و کاستی به شما منتقل خواهم کرد تا از یک برنامه‌نویس تئوری‌دان به یک متخصص عملیاتی تبدیل شوید.

18. تعامل با دیزاین پروژه «ژلاتو هاوس» در محیط فیگما
    در این بخش از دوره آموزشی، فرآیند ورود به یک تیم حرفه‌ای و نحوه تعامل با دیزاین پروژه «ژلاتو هاوس» در محیط فیگما (Figma) بررسی شده است. در ادامه، تمامی نکات مطرح شده در این ویدیو به صورت کامل و دسته‌بندی شده ارائه می‌شود:

ورود به تیم و محیط فیگما

فرآیند کار به این صورت شروع می‌شود که شما پس از ساخت اکانت در فیگما، ایمیل خود را به دیزاینر تیم می‌دهید. دیزاینر لینکی را برای شما ارسال می‌کند که با اکسپت کردن آن، به فضای طراحی پروژه دسترسی پیدا می‌کنید. در این صفحه، کل سایت ژلاتو هاوس شامل نسخه‌های دسکتاپ و موبایل دیزاین شده است. حتی اگر بخش‌هایی از دیزاین نامرتب به نظر برسد، کلیت کار برای توسعه آماده است. دیزاینر با استفاده از تگ‌هایی مانند Ready for Dev به شما اطلاع می‌دهد که طرح نهایی شده و دیگر تغییر نخواهد کرد.

تحلیل اولیه و انتخاب تکنولوژی (Tech Stack)

به عنوان یک توسعه‌دهنده فرانت‌اند، اولین قدم پس از مشاهده دیزاین، بررسی المان‌ها و انتخاب ابزارهای مناسب برای توسعه است:

انتخاب فریم‌ورک: در این پروژه، مبنا بر استفاده از React است. در دنیای واقعی، گاهی تیم از قبل استک فنی را مشخص کرده (مثلاً فقط Angular یا Vue) و گاهی بر اساس مهارت شما تصمیم‌گیری می‌شود.
بررسی جزئیات ظاهری: با کلیک بر روی المان‌ها در فیگما، متوجه جزئیاتی مثل فونت (که در اینجا وزیر متن است) و ویژگی‌های استایل می‌شوید.
پروتوتایپ و رفتار سایت: با استفاده از قابلیت Play در فیگما، می‌توانید نحوه اسکرول شدن سایت، المان‌های Sticky (چسبنده) مثل هدر، و پاپ‌آپ‌ها یا انیمیشن‌های خاص (مثل حالت نبض‌تور) را مشاهده کنید تا درک درستی از رفتار تعاملی سایت داشته باشید.
استراتژی انتخاب پکیج‌ها و کتابخانه‌ها

یکی از مهم‌ترین تصمیمات، انتخاب ابزارهای CSS است. آیا باید از Tailwind استفاده کرد یا کتابخانه‌های آماده‌ای مثل MUI (Material UI) یا Ant Design؟

اگر دیزاینر از المان‌های استاندارد متریال استفاده کرده باشد، منطقی است که از پکیج MUI استفاده کنید تا سرعت کار بالا برود. اما اگر دیزاینر اینپوت‌ها و المان‌های اختصاصی طراحی کرده باشد، استفاده از کتابخانه‌های آماده و شخصی‌سازی سنگین آن‌ها توصیه نمی‌شود، زیرا نگهداری کد را برای توسعه‌دهندگان بعدی سخت می‌کند.
اصل عدم تداخل: نباید چندین کتابخانه کامپوننت (مثلاً MUI و Ant Design) را با هم ترکیب کرد، چون سینک کردن دیزاین‌سیستم‌های متفاوت در یک پروژه چالش‌برانگیز است. البته استفاده از تلویند در کنار برخی کتابخانه‌ها (مثل HeroUI یا در نسخه‌های جدید Ant Design) به دلیل هم‌پوشانی فنی، رایج و منطقی‌تر شده است.
مهندسی و استفاده از ابزارهای آماده

یک برنامه نویس حرفه‌ای نباید "چرخ را از اول اختراع کند". برای بخش‌هایی مثل اسلایدر یا کروسل، بهتر است از پکیج‌های معتبری مانند Swiper استفاده شود. این پکیج‌ها طی سال‌ها تست شده‌اند و مشکلاتی که ممکن است در مرورگرهای مختلف (مثل باگ‌های خاص سافاری) پیش بیاید را حل کرده‌اند. وظیفه مهندس، شناخت ابزار درست و استفاده صحیح از آن است، نه خلق دوباره ابزارهایی که نمونه عالی آن‌ها وجود دارد.

مفهوم دیزاین‌سیستم (Design System)

تیم‌های بزرگ مانند اسنپ، دیجی‌کالا یا علی‌بابا، دارای دیزاین‌سیستم اختصاصی هستند. دیزاین‌سیستم شامل دستورالعمل‌های دقیق برای رنگ‌ها، تایپوگرافی، شکل باتن‌ها، شدوها و مدال‌هاست.

فایده دیزاین‌سیستم این است که وقتی یک زیرمجموعه جدید (مثلاً اسنپ‌فود یا اسنپ‌کاپ) اضافه می‌شود، ساختار اصلی تغییر نمی‌کند و فقط تم رنگی عوض می‌شود تا یکپارچگی برند حفظ شود.
به عنوان توسعه‌دهنده، شما باید به دیزاین‌سیستم تیم احترام بگذارید و نمی‌توانید سلیقه شخصی یا ابزاری که صرفاً خودتان به آن مسلط هستید را به تیم تحمیل کنید.
نقشه راه پروژه ژلاتو هاوس

در این پروژه آموزشی، رویکرد به شرح زیر خواهد بود:

۱. ءCSS: استفاده از Tailwind CSS به عنوان ابزار اصلی (Utility-first)

۲. طراحی اختصاصی: برای یادگیری بهتر، گریدبندی‌ها و المان‌های رسپانسیو را خودمان با تلویند می‌نویسیم و از کتابخانه‌های آماده کامپوننت استفاده نمی‌کنیم

۳. مدیریت وضعیت: استفاده از Zustand

۴. ارتباط با سرور: استفاده از React Query

۵. فرآیند توسعه: ابتدا ساختار کلی و دیزاین را پیاده‌سازی می‌کنیم و در مراحل بعدی به API متصل می‌شویم.

در نهایت، پس از بررسی کامل دیزاین و ابزارها، توسعه‌دهنده باید تخمین زمانی خود را به اسکرام مستر یا مدیر پروژه اعلام کند (مثلاً مشخص کند پیاده‌سازی اسلایدر یا بخش‌های دیگر چند روز زمان می‌برد). این توانایی در تخمین زمان، یکی از مهارت‌های کلیدی در کار تیمی است.

19. راه اندازی پروژه
    در این بخش از آموزش، فرآیند راه‌اندازی پروژه «ژلاتو هاوس» را از صفر شروع می‌کنیم و تمامی مراحل فنی از ایجاد پروژه تا انتشار آن در گیت‌هاب را بررسی می‌کنیم.

انتخاب ابزارها: چرا Vite و pnpm؟

برای شروع، از کتابخانه ریاکت استفاده می‌کنیم، اما به جای روش‌های قدیمی مثل create-react-app (CRA)، از ابزار مدرن Vite بهره می‌بریم. ویت یک «بیلد تولز» بسیار سریع است که تمام تنظیمات پیچیده مانند وب‌پک، بابل و پلاگین‌های مورد نیاز را به صورت خودکار انجام می‌دهد. این ابزار به جای بابل از SWC استفاده می‌کند که با زبان Rust نوشته شده و تا ۵۰ برابر سریع‌تر عمل می‌کند، که نتیجه آن سرعت بسیار بالاتر در هنگام ذخیره تغییرات و توسعه پروژه است.

برای مدیریت پکیج‌ها نیز از pnpm استفاده می‌کنیم. مزیت اصلی pnpm این است که پکیج‌ها را روی سیستم شما کش می‌کند؛ بنابراین در پروژه‌های بعدی نیازی به دانلود مجدد آن‌ها نیست که این موضوع برای سرعت کار و صرفه‌جویی در ترافیک اینترنت بسیار مفید است.

ایجاد پروژه و تنظیمات اولیه

پس از باز کردن ویژوال استودیو کد و ایجاد فولدر پروژه، با استفاده از دستور pnpm create vite فرآیند ساخت را آغاز می‌کنیم. نام پروژه را gelato-frontend گذاشته و در مراحل بعد گزینه‌های React و سپس TypeScript + SWC را انتخاب می‌کنیم. اگرچه نسخه‌های جدیدتر ریاکت (مثل نسخه ۱۹) دارای کامپایلر جدیدی هستند، اما فعلاً برای پایداری بیشتر از همان نسخه استاندارد SWC استفاده می‌کنیم. همچنین از انتخاب گزینه‌های آزمایشی مانند Rolldown در این مرحله خودداری می‌کنیم تا در محیط پروداکشن دچار مشکل نشویم.

نصب وابستگی‌ها و تمیزکاری پروژه

بعد از ایجاد ساختار، با دستور pnpm install تمام کتابخانه‌های مورد نیاز نصب می‌شوند. سپس برای شروع یک کدنویسی تمیز، فایل‌های اضافی که به صورت پیش‌فرض توسط ویت ایجاد شده‌اند (مانند لوگوها، کدهای داخل App.tsx و استایل‌های اضافی در index.css) را کاملاً پاک می‌کنیم. در فایل main.tsx ایمپورت‌های بلااستفاده را حذف کرده و تایتل پروژه را در فایل index.html به Gelato House تغییر می‌دهیم. در نهایت با دستور pnpm dev پروژه را در مرورگر بالا می‌آوریم تا از صحت اجرای آن مطمئن شویم.

راه‌اندازی گیت‌هاب و مدیریت دسترسی‌ها

در دنیای حرفه‌ای، معمولاً مدیر فنی یا مدیر پروژه یک مخزن (Repository) در گیت‌هاب ایجاد کرده و به شما دسترسی می‌دهد. پس از دریافت ایمیل دعوت و تایید آن، نوبت به اتصال پروژه لوکال به گیت‌هاب می‌رسد.

ابتدا دستور git init را اجرا می‌کنیم تا گیت در پروژه فعال شود. نکته بسیار حیاتی این است که فایل .gitignore حتماً شامل پوشه node_modules باشد؛ چون این پوشه حجم بسیار بالایی دارد و نباید روی گیت‌هاب آپلود شود، بلکه هر کسی پروژه را کلون کند می‌تواند با یک دستور نصب، آن را دوباره بسازد.

حل چالش‌های رایج در گیت (خطای ۴۰۳)

هنگام اتصال پروژه به ریموت با دستور git remote add origin و تلاش برای اولین پوش (git push)، ممکن است با خطای 403 Permission Denied مواجه شوید. این خطا اغلب زمانی رخ می‌دهد که چندین اکانت گیت‌هاب روی یک سیستم داشته باشید و اطلاعات اکانت قبلی در حافظه سیستم عامل مانده باشد. برای حل این مشکل در ویندوز، باید به بخش Credentials Manager رفته و اطلاعات قبلی گیت‌هاب را پاک کنید تا سیستم دوباره از شما یوزرنیم و پسورد بخواهد.

همچنین برای اینکه نام و ایمیل شما در تاریخچه کامیت‌ها درست نمایش داده شود، باید با دستورات git config مشخصات خود را به صورت گلوبال تنظیم کنید.

جریان کاری (Git Flow)

به عنوان یک تمرین اصولی، پروژه را روی برنچ dev توسعه می‌دهیم. این کار به ما کمک می‌کند تا همیشه یک نسخه پایدار و نهایی روی برنچ master داشته باشیم و کدهای در حال توسعه و آزمایش را در برنچ دیگری مدیریت کنیم. در نهایت، پس از هر تغییر، کدها را با دستورات add و commit ذخیره کرده و به گیت‌هاب می‌فرستیم تا همواره یک نسخه پشتیبان از زحماتمان در دسترس باشد.

با انجام این مراحل، زیرساخت پروژه ما کاملاً آماده است و می‌توانیم در بخش‌های بعد وارد دنیای جذاب کدنویسی کامپوننت‌ها و پیاده‌سازی دیزاین شویم.

20. کانفیگ tailwindcss به همراه postcss
    در این بخش ، پس از آماده‌سازی ساختار اولیه پروژه و آپلود آن در گیت، نوبت به مدیریت استایل‌ها و راه‌اندازی زیرساخت طراحی می‌رسد. به عنوان یک توسعه‌دهنده فرانت‌اند، پس از تحلیل دیزاین، اولین قدم نصب پکیج‌های مورد نیاز برای پیاده‌سازی ظاهر سایت است. انتخاب اصلی ما در این پروژه Tailwind CSS است و در کنار آن ممکن است از کتابخانه‌هایی مثل shadcn/ui برای افزایش سرعت در پیاده‌سازی کامپوننت‌هایی مثل تب‌ها و اینپوت‌ها استفاده کنیم.

نصب و کانفیگ Tailwind CSS در پروژه ویت (Vite)

برای نصب تلویند، نیازی به حفظ کردن دستورات نیست؛ کافی است به مستندات رسمی تلویند بخش ویت مراجعه کنید. مراحل به این صورت پیش می‌رود: ۱. ابتدا پکیج‌های مورد نیاز تلویند را با استفاده از مدیریت پکیج (در اینجا pnpm) نصب می‌کنیم. ۲. سپس فایل vite.config.ts را باز کرده و پلاگین تلویند را به آن اضافه می‌کنیم. با استفاده از قابلیت اینتلیسنس (IntelliSense) ویژوال استودیو کد، این پلاگین را ایمپورت می‌کنیم. ۳. یک پوشه به نام styles در بخش src ایجاد می‌کنیم تا استایل‌های گلوبال پروژه را در آن مدیریت کنیم. فایلی به نام tailwind.css در این پوشه ساخته و دستورات اصلی تلویند (@tailwind base, @tailwind components, @tailwind utilities) را در آن قرار می‌دهیم. ۴. در نهایت، این فایل CSS را در فایل اصلی پروژه یعنی main.tsx ایمپورت می‌کنیم تا در تمام پروژه در دسترس باشد.

تست و تایید صحت نصب

برای اطمینان از اینکه تلویند به درستی متصل شده است، یک کلاس تلویند مثل bg-red-300 را به یکی از المان‌ها می‌دهیم. اگر رنگ پس‌زمینه تغییر کرد، یعنی کانفیگ با موفقیت انجام شده است. یکی از ویژگی‌های مهم تلویند این است که به صورت خودکار عمل Reset را انجام می‌دهد؛ یعنی نیازی نیست خودتان به صورت دستی مارجین، پدینگ و باکس‌سایزینگ را صفر کنید، زیرا تلویند این تنظیمات پیش‌فرض را در بخش base خود دارد.

فلسفه و مزایای استفاده از Tailwind CSS

ایده اصلی تلویند افزایش سرعت توسعه است. برخلاف روش‌های سنتی که باید مدام بین فایل‌های HTML و CSS جابه‌جا می‌شدید، در تلویند استایل‌ها را مستقیماً در کنار کد جاوا اسکریپت و ساختار پروژه می‌نویسید. این کار باعث می‌شود کامپوننت‌ها کاملاً ایزوله باشند و استایل یک بخش بر بخش دیگر تاثیر ناخواسته نگذارد (Overriding اتفاق نمی‌افتد). از نظر عملکردی نیز تلویند بسیار بهینه است؛ در هنگام بیلد گرفتن، تمام کلاس‌های اضافی و استفاده نشده حذف می‌شوند و فقط کدهای CSS مورد نیاز شما در خروجی نهایی قرار می‌گیرند که باعث کاهش حجم پروژه می‌شود.

تلویند شباهت‌هایی به بوت‌استرپ دارد اما در سطح پایین‌تر (Utility-first) عمل می‌کند. برای استفاده حرفه‌ای از آن، باید دانش CSS خوبی داشته باشید تا بتوانید کلاس‌ها را به درستی ترکیب کنید. اکثر کلاس‌ها نام‌های آشنایی دارند، مثلاً برای بردر ردیوس از کلاس rounded استفاده می‌شود.

افزودن PostCSS برای سازگاری با مرورگرهای قدیمی

در این پروژه، ما PostCSS را نیز نصب و کانفیگ می‌کنیم. پست‌سی‌اس‌اس برای CSS همان کاری را انجام می‌دهد که Babel یا SWC برای جاوا اسکریپت انجام می‌دهند؛ یعنی کدهای مدرن شما را به کدهایی تبدیل می‌کند که برای مرورگرهای قدیمی (مثلاً تلویزیون‌های هوشمند قدیمی مدل ۲۰۱۳ یا ۲۰۱۵ که مرورگرشان آپدیت نمی‌شود) قابل فهم باشد. برای این کار، پکیج‌های autoprefixer و postcss را نصب کرده و فایل کانفیگ postcss.config.js را در ریشه پروژه ایجاد می‌کنیم. طبق مستندات جدید، می‌توان فایل CSS اصلی را حتی مستقیماً در index.html نیز آدرس‌دهی کرد تا از قابلیت‌های جدیدتر بهره‌مند شد.

مدیریت تغییرات در گیت

پس از اتمام نصب و کانفیگ، تغییرات را در گیت ذخیره می‌کنیم. یک نکته فنی مهم در گیت این است که اگر فایل‌های جدیدی به پروژه اضافه کرده باشید، دستور git commit -am آن‌ها را شناسایی نمی‌کند. بنابراین همیشه بهتر است ابتدا با git add . تمام فایل‌های جدید و تغییر یافته را استیج کنید و سپس با git commit -m پیام خود را بنویسید. در نهایت با دستور git push کدها را به مخزن ریموت (مثلاً برنچ dev در گیت‌هاب) ارسال می‌کنیم.

این زیرساخت اکنون مانند تکه‌های پازل در کنار هم قرار گرفته‌اند تا یک مهندس فرانت‌اند بتواند پروژه‌ای پایدار، سریع و سازگار با انواع دیوایس‌ها تولید کند.

21. دیپلوی کردن پروژه روی vercel و درباره CI/CD
    در این بخش از آموزش پروژه ژلاتو هاوس، به سراغ یکی از هیجان‌انگیزترین مراحل توسعه می‌رویم؛ یعنی زنده کردن پروژه روی اینترنت تا تمامی تغییرات به صورت لحظه‌ای و لایو برای همه قابل مشاهده باشد.

اتصال پروژه به ورسل (Vercel)

برای اینکه بتوانید خروجی کار خود را روی اینترنت منتشر کنید، از پلتفرم قدرتمند ورسل (Vercel.com) استفاده می‌کنیم. فرآیند کار بسیار ساده است: ابتدا در سایت ورسل لاگین کرده و پروژه خود را از گیت‌هاب ایمپورت می‌کنیم. ورسل به طور هوشمند تشخیص می‌دهد که پروژه ما با استفاده از ابزار Vite ساخته شده است. تنها با زدن دکمه Deploy، ورسل شروع به نصب پیش‌نیازها، گرفتن بیلد (Build) از پروژه و در نهایت اختصاص یک دامین رایگان به آن می‌کند. همچنین این امکان وجود دارد که دامین اختصاصی خود را (مثلاً بر روی یک زیردامنه شخصی) به پروژه متصل کنید.

مفهوم CI/CD و مزایای آن

با اتصال پروژه به ورسل، ما عملاً یک چرخه CI/CD (مخفف Continuous Integration و Continuous Deployment) راه انداخته‌ایم. این به زبان ساده یعنی «یکپارچه‌سازی و انتشار مداوم». از این به بعد، هر بار که شما کدی را در سیستم خود تغییر دهید و آن را به گیت‌هاب کامیت و پوش (Push) کنید، ورسل به صورت خودکار متوجه تغییرات شده، نسخه جدید را بیلد می‌گیرد و روی سرور جایگزین نسخه قبلی می‌کند. این کار بدون دخالت دستی انجام می‌شود و همیشه آخرین نسخه کد شما روی اینترنت در دسترس است.

مدیریت گیت در محیط ویژوال استودیو کد

برای کار با گیت، علاوه بر دستورات ترمینال، می‌توان از محیط گرافیکی (UI) خود VS Code نیز استفاده کرد. مراحل به این صورت است:

۱. تغییرات را در بخش Source Control مشاهده می‌کنیم. ۲. با زدن علامت مثبت (+) تغییرات را به مرحله Stage می‌بریم (معادل دستور git add). ۳. یک پیام کامیت استاندارد می‌نویسیم. بهتر است از استایل‌های مشخصی برای کامیت استفاده کنیم؛ مثلاً ابتدا کلمه فیکس یا چنج، سپس نام فایل و در نهایت توضیح کوتاهی از کار انجام شده. ۴. در نهایت با زدن دکمه Sync یا Push، کدها به گیت‌هاب فرستاده می‌شوند.

تست عملی تغییرات لایو

در این ویدیو برای تست، متن "Hello World" به "Gelato House" تغییر داده شد. بلافاصله پس از پوش کردن کد، ورسل در کمتر از چند ثانیه فرآیند دپلوی مجدد را آغاز کرد. با رفرش کردن لینک سایت، مشاهده کردیم که تغییرات اعمال شده و متن جدید نمایش داده می‌شود. در گیت‌هاب نیز یک تیک سبز رنگ در کنار کامیت ظاهر می‌شود که نشان‌دهنده موفقیت‌آمیز بودن عملیات دپلوی توسط ورسل است.

بررسی نسخه پروداکشن (Production Build)

وقتی سایت شما توسط ورسل بالا می‌آید، در واقع نسخه «پروداکشن» ریاکت در حال اجراست. این موضوع را می‌توان از طریق افزونه React Developer Tools در مرورگر متوجه شد؛ پیامی نمایش داده می‌شود مبنی بر اینکه صفحه از نسخه بهینه و نهایی ریاکت استفاده می‌کند. این یعنی کدها فشرده شده، حجم آن‌ها کاهش یافته و کاملاً برای استفاده کاربر نهایی آماده هستند.

از این مرحله به بعد، هر قطعه کدی که برای ژلاتو هاوس بنویسیم، بلافاصله روی آدرس اینترنتی پروژه منتشر می‌شود و این احساس فوق‌العاده‌ای است که خروجی کار خود را در محیط واقعی و خارج از سیستم شخصی (Local) مشاهده کنید.

22. درباره Layout و Wiring و تایپ‌بندی کامپوننت‌ها با TypeScript
    در این بخش از آموزش پروژه «ژلاتو هاوس»، وارد فضای واقعی یک تیم نرم‌افزاری می‌شویم و یاد می‌گیریم که چگونه ساختار کلی و قالب اصلی (Layout) یک اپلیکیشن را طراحی و پیاده‌سازی کنیم. در ادامه، تمامی نکات فنی و مدیریتی مطرح شده در این ویدیو به صورت کامل بررسی شده است.

تعاملات تیمی و مدیریت تسک‌ها

در یک محیط حرفه‌ای، توسعه‌دهنده به تنهایی کار نمی‌کند. تیم پروداکت شامل نقش‌هایی مثل مدیر محصول (Product Manager)، طراح محصول و اسکرام مستر (Scrum Master) است. اسکرام مستر وظیفه دارد تسک‌ها را در بازه‌های زمانی دو هفته‌ای که به آن «اسپرینت» (Sprint) می‌گویند، مدیریت کند. او گزارش پیشرفت کارها و پرفورمنس شما را به مدیر محصول می‌دهد تا بیزینس بتواند بر اساس آن برای ارائه فیچرهای جدید به مشتریان برنامه‌ریزی کند. برای مستندسازی و مدیریت این تسک‌ها، ابزارهایی مثل جیرا (Jira) یا ترلو (Trello) که از سیستم «کنبن» (Kanban) استفاده می‌کنند، بسیار رایج هستند. با این حال، در این پروژه از ابزار سبک‌تر و منعطف‌تری به نام Notion برای مدیریت تسک‌ها استفاده شده است.

مفهوم وایر کردن (Wiring)

یک اصطلاح تخصصی که در این ویدیو با آن آشنا می‌شویم، «وایر کردن» است. این مرحله زمانی اتفاق می‌افتد که دیزاین آماده است اما هنوز ای‌پی‌آی‌ها (APIs) توسط برنامه نویس بک‌ئند تحویل داده نشده است. در این فاصله، برنامه نویس فرانت‌ئند ظاهر سایت را پیاده‌سازی کرده و زیرساخت‌ها را به شکلی آماده می‌کند که به محض آماده شدن بک‌ئند، پروژه به راحتی به دیتابیس متصل شود.

طراحی ساختار (Layout)

اولین قدم برای شروع کدنویسی هر قالبی، شناسایی و ساخت لایوت است. لایوت شامل بخش‌های تکراری سایت است که در تمام صفحات ثابت هستند؛ مانند هدر (Header) و فوتر (Footer). در حالی که هدر و فوتر ثابت می‌مانند، محتوای میانی (Content) بر اساس صفحه‌ای که کاربر در آن حضور دارد، تغییر می‌کند. در ساختار پروژه، یک پوشه به نام components و درون آن پوشه‌ای به نام layout ایجاد می‌کنیم. با نام‌گذاری فایل اصلی به صورت index.tsx در پوشه لایوت، آدرس‌دهی و ایمپورت کردن آن در سایر فایل‌ها ساده‌تر و تمیزتر می‌شود.

قدرت پراپس Children در ری اکت

برای اینکه بخش میانی لایوت را داینامیک کنیم، از ویژگی children در ریاکت استفاده می‌کنیم. با گرفتن این پراپس در کامپوننت لایوت و قرار دادن آن در تگ <main>، هر محتوایی که بین تگ‌های باز و بسته لایوت قرار بگیرد، به صورت خودکار در همان قسمت نمایش داده می‌شود. این کار باعث می‌شود هدر و فوتر همیشه در جای خود باقی بمانند و فقط محتوای اصلی تغییر کند.

import type React from "react";
import { Footer } from "./footer";
import { Header } from "./header";
import type { PropsWithChildren } from "react";

export const Layout: React.FC<PropsWithChildren> = ({ children }) => {
return (
<>

<Header />
<main>{children}</main>
<Footer />
</>
);
};
قانون والد واحد و معرفی فرگمنت (Fragment)

یکی از چالش‌های مهم در ریاکت این است که هر کامپوننت در هنگام بازگشت (return) حتماً باید یک «والد واحد» داشته باشد. یعنی نمی‌توانید چند المنت (مثل هدر، فوتر و مین) را به صورت موازی و بدون داشتن یک والد مشترک برگردانید. دلیل این موضوع به ساختار داده‌ای DOM برمی‌گردد که به صورت یک درخت (Tree) است و هر گره (Node) باید به یک گره والد متصل باشد. همچنین ریاکت به عنوان «منبع واحد حقیقت» (Source of Truth)، نیاز دارد که همه اجزای یک کامپوننت را به یک ریشه واحد متصل کند. برای حل این مشکل بدون اضافه کردن تگ‌های اضافی مثل <div> (که باعث شلوغی DOM می‌شود)، از React Fragment استفاده می‌کنیم. سینتکس کوتاه آن به صورت تگ‌های خالی <> ... </> است که به ما اجازه می‌دهد المنت‌های موازی را بدون ایجاد والد فیزیکی در خروجی، در کنار هم قرار دهیم.

تایپ‌بندی کامپوننت‌ها با TypeScript

از آنجایی که پروژه با تایپ‌اسکریپت پیش می‌رود، باید به کامپوننت‌ها هویت و تایپ مشخص بدهیم. برای این کار از نوع React.FC (مخفف Functional Component) استفاده می‌کنیم. همچنین برای کامپوننت‌هایی که دارای فرزند هستند، از جنریک PropsWithChildren استفاده می‌کنیم تا تایپ‌اسکریپت به درستی متوجه شود که این کامپوننت قرار است پراپس children را دریافت کند. این کار باعث جلوگیری از خطاهای احتمالی در هنگام توسعه و افزایش دقت کدنویسی می‌شود.

با پیاده‌سازی این زیرساخت، قالب اصلی سایت آماده شده و در مراحل بعدی می‌توان دانه دانه اجزای داخلی هر بخش را تکمیل کرد تا پیش‌نمایش نهایی سایت شکل بگیرد.
